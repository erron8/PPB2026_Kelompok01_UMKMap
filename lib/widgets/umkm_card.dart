import 'package:flutter/material.dart';

class UmkmCard extends StatelessWidget {
  const UmkmCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(child: SizedBox.shrink());
  }
}
