import 'package:flutter/material.dart';

class CompassArrow extends StatelessWidget {
  const CompassArrow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.navigation);
  }
}
