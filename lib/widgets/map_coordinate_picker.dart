import 'package:flutter/material.dart';

class MapCoordinatePicker extends StatelessWidget {
  const MapCoordinatePicker({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
