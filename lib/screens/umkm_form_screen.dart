import 'package:flutter/material.dart';

class UmkmFormScreen extends StatelessWidget {
  const UmkmFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Form UMKM')));
  }
}
