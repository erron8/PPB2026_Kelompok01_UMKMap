import 'package:flutter/material.dart';

class UmkmListScreen extends StatelessWidget {
  const UmkmListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Daftar UMKM')));
  }
}
